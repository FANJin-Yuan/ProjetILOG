var express = require('express');
var mongoose = require('mongoose');

var app = express();

app.get('/', (req,res) => {res.send('salut')});

console.log('Project4 lancé sur le port 80')

app.listen(80);
